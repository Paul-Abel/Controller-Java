<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tv_remote">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".alarmClock.AlarmClockList" />
        <activity android:name=".alarmClock.AlarmClock" />
        <activity
            android:name=".pcRemote.AlertPasswordDialog"
            tools:ignore="Instantiatable" />
        <activity android:name=".pcRemote.Keyboard" />
        <activity android:name=".pcRemote.pcRemoteControl" />
        <activity android:name=".TVRemote2" />
        <activity android:name=".LedCupboard" />
        <activity android:name=".Receiver" />
        <activity android:name=".LedTable" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>